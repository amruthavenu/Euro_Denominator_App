package org.example.euro_denominator;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
/**
 * Main entry point for the Euro Denomination Spring Boot application.
 */
@SpringBootApplication
public class EuroDenominatorApplication {
    public static void main(String[] args) {
        SpringApplication.run(EuroDenominatorApplication.class, args);
    }
}
